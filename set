#start project
Sets = {"empty"}

Sets.remove("empty")

addFruits = input("add fruit :")

Sets.add(addFruits)

for s in Sets:
    addFruits = input("add fruit :")
    Sets.add(addFruits)
    print(Sets) 

#end
